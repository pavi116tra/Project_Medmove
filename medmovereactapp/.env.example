# Production Backend URL (Render URL)
# Example: https://medmove-backend.onrender.com
REACT_APP_API_URL=https://your-backend-url-on-render.com

# For local development, this will fallback to localhost:5000 automatically
# in src/config/api.js, so you only need to set this on Netlify dashboard.
